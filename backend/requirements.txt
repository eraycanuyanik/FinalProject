# --- Phase 1: skeleton / service wiring ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Later phases (kept here so the image is built once). Commented until used. ---
# python-multipart==0.0.20      # file uploads (Phase 2)
# pdfplumber==0.11.5            # PDF text extraction (Phase 2)
# python-docx==1.1.2            # DOCX extraction (Phase 2)
# pytesseract==0.3.13           # OCR fallback (Phase 2)
# sentence-transformers==3.3.1  # embeddings (Phase 4)
# chromadb==0.5.23              # vector store client (Phase 4)
# beautifulsoup4==4.12.3        # mevzuat scraping (Phase 4)

# --- Tests ---
pytest==8.3.4
